# 积分系统 更新报告 · v5.21.39

- **版本**：网页 `v5.21.39` ＋ 插件 `5.21.39`
- **日期**：2026-09-13
- **范围**：astrbot_plugin_points（机器人侧）＋ points_web（网站侧）

---

## 一、身份系统（本次最大的改动）

| 概念 | 说明 |
| --- | --- |
| **cookieQQ** | 注册验证码，仅 NapCat **私聊** `/积分 cookie` 获取（每10分钟更新） |
| **永久 UID** | 账号主键，7 位、首位固定 1、生成后不变；网站显示、转账/查询都用它 |
| **openid** | QQ 官方机器人渠道的身份 |

**绑定流程**
```
NapCat 私聊 /积分 cookie  →  拿到验证码
        ↓
网站注册                  →  得到永久 UID
        ↓
官方机器人 /积分 绑定 <UID>  →  网站确认/拒绝
        ↓
确认后：各渠道归到同一个 UID，历史积分自动合并
```

- 官方机器人**未绑定 UID 前，所有积分指令会被拦截**并提示绑定步骤（`/积分 帮助`、`/积分 菜单` 放行）；
- 新增 **`/积分 我的`**：查看账号信息（渠道身份 / 绑定状态 / 积分 / 会员），支持
  - `解绑本渠道`（只解当前渠道）
  - `注销绑定`（解除该 UID 的所有渠道绑定，**网站账号保留**，均需二次确认）

---

## 二、解析链：UID → QQ号 → QQ昵称

```
UID 1187405 → 网站账号 → 注册QQ 3697553866 → NapCat昵称 薇薇丫
                         ↓
显示效果：薇薇丫（QQ 3697553866）
```
已用于：**转账**、**查询**、**图片作者**、**转账流水备注**。

---

## 三、菜单系统（两级 + 分区）

```
/积分 帮助（或 /积分 菜单）
   ↓
总菜单（8 大分类）
[积分功能][图片功能][漂流瓶][积分商城]
[红包转账][其它功能][我的账号][网站功能]
[网站主站]🔗 [照片墙]🔗
   ↓ 点「图片功能」
分菜单：该分类的说明文本 + 该分类专属按钮
```

**分区键盘**：按当前指令自动切换

| 指令 | 用哪套按钮 |
| --- | --- |
| `/积分 帮助`、`/积分 菜单` | 总菜单（8 个分类 + 2 个链接） |
| `/积分 菜单 图片`、`/积分 图片 xxx` | 图片（上传/我的/随机/信息/链接/赞赏/删除/照片墙） |
| `/积分 菜单 商城`、`/积分 商城 xxx` | 商城 |
| `/积分 菜单 红包` | 红包转账 |
| `/积分 菜单 其它` | 其它（会员/存档/回档/备份） |
| `/积分 菜单 我的` | 我的账号（解绑/注销/我的图片/红包/订单） |
| `/积分 菜单 网站` | 网站功能（12 个跳转链接，带 hash 路由） |
| `/漂流瓶 xxx`、`/捡漂流瓶` | 漂流瓶（投出/匿名/捡/评论/举报…） |
| 普通积分指令 | 积分常用（13 个） |

**管理员菜单**（三层保护：看不到 / 进不去 / 命令本身校验）

| 分类 | 按钮 |
| --- | --- |
| 积分管理 | 设置 / 增加 / 扣除 / 拉黑 / 解除拉黑 |
| 商城管理 | 添加商品 / 删除商品 / 订单列表 / 发货 |
| 账号管理 | 创建 / 列表 / 改密码 / 禁用 / 启用 / 删除 |
| 数据管理 | 存档 / 回档 / 立即备份 / 备份列表 / 回档备份 |
| 瓶子管理 | 待审 / 通过 / 下架 / 封禁 / 举报列表 / 待审评论 |

**未绑定 UID 时**：官方机器人**只显示一个「绑定账号」按钮**；NapCat 正常显示并在末尾补「绑定账号」。

---

## 四、快捷按钮（自动带入参数）

| 操作完成后 | 唯一按钮 | 自动填入 |
| --- | --- | --- |
| 发红包 | 抢红包 | `/积分 抢红包 hbXXXX` |
| 上传图片 | 删除这张 | `/积分 图片 删除 图片ID` |
| 投出漂流瓶 | 评论这个瓶子 | `/漂流瓶 评论 编号 ` |
| 捡漂流瓶 | 评论这个瓶子 | `/漂流瓶 评论 编号 ` |
| 随机/查看图片 | 赞赏这张 | `/积分 图片 赞赏 图片ID ` |

**技术点**：单条消息自定义按钮（`event.set_extra("points_btn_override", ...)`）。
另外 `/积分 抢红包` 不带 ID 时**自动抢最新的未抢完红包**。

> ⚠️ 图片消息挂不了按钮，所以"随机/查看图片"改为**先发文字（带按钮）再发图片**。

---

## 五、漂流瓶

- 存储从 jsonbin 换成 **GitHub 仓库**（`integration-system/data/bottles.json`）；
- 卡片作者/评论显示 **昵称 + QQ 头像**（以前是原始 ID 且评论无头像）；
- 评论渲染新增头像、名字走解析链；匿名瓶保持"匿名用户"。

---

## 六、网站修复

| 问题 | 原因 | 修复 |
| --- | --- | --- |
| 聊天室里自己发的消息在左边、头像不对 | 消息存 UID、前端拿 QQ 比对 | 两端统一，兼容 UID/QQ |
| 留言板/棋局/聊天室头像错位 | `_qq_avatar_url()` 把 uid 当 QQ 号 | 改为「UID→反查账号→真实QQ」 |
| 棋局玩家卡显示"QQ：1187405" | 同上 | 状态里返回真实 QQ |
| 我的积分不显示 | 用 QQ 号查数据 | 改为按永久 UID 取数 |
| 点「网站功能」无法直达标签页 | 没有 hash 路由 | 新增 hash 路由（`/#rank` 等） |

**新增**：更新公告（首次访问自动弹出，每版本一次）
- 支持**拖动**（鼠标/触摸）、**✕ 按钮 / Esc / 点空白** 关闭；
- **移动端适配**：小屏从底部弹出、可滚动、圆角贴底。

**新增版本锁 🔒**
> 网站版本与插件版本**不一致**时，自动锁定为「离线模式」：
> 顶部提示条说明原因，积分相关功能不可用，聊天室/留言板/邮箱/反馈/五子棋仍可用。
> （防止网站/插件新老版本混跑导致数据错乱）

---

## 七、其他修复

- **`/积分帮助` 会误调用 LLM** → 命令处理完即终止事件（并且只拦截"真指令"，普通聊天不受影响）；
- 新增 `/积分帮助`、`/积分菜单`、`/积分说明`、`/漂流瓶帮助`（不带空格也能用）；
- 转账/查询统一按 **永久 UID** 口径。

---

## 八、部署清单

**网站**（pythonanywhere 控制台）
```bash
cd ~
# 方式一（推荐，走 API 不吃缓存）：单独取改动的文件
curl -sL -H "Accept: application/vnd.github.raw" \
  "https://api.github.com/repos/wwy-12393342/bot-status/contents/site_files/app.py?ref=main" \
  -o ~/points_web/app.py
curl -sL -H "Accept: application/vnd.github.raw" \
  "https://api.github.com/repos/wwy-12393342/bot-status/contents/site_files/static/app.js?ref=main" \
  -o ~/points_web/static/app.js
curl -sL -H "Accept: application/vnd.github.raw" \
  "https://api.github.com/repos/wwy-12393342/bot-status/contents/site_files/templates/index.html?ref=main" \
  -o ~/points_web/templates/index.html

# 自检
grep -c _qq_of_uid ~/points_web/app.py           # 4
grep -c userAvatar ~/points_web/static/app.js    # 3
grep -c "v5.21.39" ~/points_web/templates/index.html  # 2
```
然后 **Web → Reload**，浏览器 **Ctrl+Shift+R 强制刷新**。

**插件**：重载积分插件（版本会显示 5.21.39）。

---

## 九、遗留 / 待办

- 漂流瓶"查看某编号"的评论显示尚未走解析链（只有"捡漂流瓶"走）；
- 旧数据（改动前）里存的是原始 ID，能解析的会显示名字，解析不到的显示匿名；
- `author_qq` 这个字段名实际存的是"用户ID"，建议后续改名为 `author_id`。

---

*本报告同时作为网站"更新公告"内容，首次访问自动展示。*
